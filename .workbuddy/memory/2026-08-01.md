# 2026-08-01 工作日志

## ningning 工作台第七次迭代
- 用户反馈排期弹窗只有标题和优先级，希望增加自由备注字段；同时希望日历上方新增「碎碎念」大脑垃圾桶，自动提炼并入库。
- 已实现：
  - 在「📌 Monthly Schedule」日历上方新增「🧠 碎碎念 · 大脑垃圾桶」区域：大段文本输入框 + 「✨ 智能解析并入库」按钮 + 解析结果展示。
  - 新增 `parseBrainDump()` 函数：按换行/句号拆分条目，调用 `detectPriority()` 与 `detectDateOffset()` 自动判断 Productivity Matrix 优先级与执行日期，写入 `schedule_timeline`。
  - 优先级规则：urgent/今天/马上/今晚/必须 → Do；明天/后天/计划/整理/下周/日期 → Schedule；让别人做/快递/打电话/通知 → Delegate；可能/随便/待定/以后再说 → Delete。
  - 日期规则：今天/马上 → 今天；明天 → +1；后天 → +2；下周 X → 下周对应星期；默认 Do 今天、其它明天。
  - 日期编辑弹窗重构：每条条目现在包含「标题输入框 + 自由备注 textarea + 优先级选择器 + 删除按钮」；新增任务区也包含标题和备注两个输入框。
  - 数据模型 `schedule_timeline` 新增 `notes` 字段；初始示例数据同步补齐 `notes: ''`。
  - 新增 `.brain-dump-card`、`.brain-dump-result` 等 CSS，适配桌面与手机端。
  - JS 语法检查通过，已重新提交并部署到 GitHub Pages。

## ningning 工作台第六次迭代
- 用户反馈排期舱希望改为「点击日历日期直接编辑」：
  - 移除控制台主页顶部「手动排期舱 · Eisenhower Matrix」表单（日期选择器、优先级下拉、任务输入、添加按钮）。
  - 新增日期任务编辑弹窗（modal）：点击日历任意日期即弹出，标题显示 `2026年8月1日 任务`。弹窗内列出该日期所有任务，每条任务可直接编辑文字与 Productivity Matrix 优先级（Do/Schedule/Delegate/Delete）。底部提供新任务输入区与添加按钮。
  - 修改任务文字或优先级时实时保存到 `schedule_timeline` 并自动上传云端；删除任务即时刷新日历圆点。
  - 保留月历下方的四象限图例，标题统一为「📌 Monthly Schedule」。
  - 新增 modal 专属 CSS，适配桌面与手机端（圆角卡片、遮罩、响应式布局）。
- 已重新提交并部署到 GitHub Pages。

## ningning 工作台第五次迭代
- 按截图反馈继续优化 Productivity Matrix：
  - 优先级选择器改为只显示「颜色圆点 + 英文」（🔴 Do / 🔵 Schedule / 🟡 Delegate / ⚪ Delete）。
  - 月历下方图例改为「颜色圆点 + 英文（中文解释）」，并删除旧的「官方待办」等残留概念。
  - 四象限颜色统一为莫兰迪浅色系：浅玫瑰 #D9A6A6、浅蓝灰 #ADC4CE、浅米黄 #D4C9A9、浅灰 #C5C1BA；同步更新 CSS 日期圆点与任务卡片标签底色。
- 初始化本地 Git 仓库，新增 `deploy.sh` 一键部署脚本与 `DEPLOY_GUIDE.md` 完整指南，覆盖：
  - GitHub CLI 认证登录步骤
  - 自动创建仓库 + 推送 + 开启 GitHub Pages
  - Safari/Chrome 添加到 Mac 程序坞的方法
  - 数据互通的四种方案（JSON 手动导出/导入、Firebase 实时同步、Supabase、GitHub Gist）
- 向用户说明：当前无服务器密钥时，跨设备互通最简单的过渡方式是 JSON 导出/导入；真正实时同步需注册 Firebase 并把 apiKey 等配置发给我接入。
- 用户运行 `deploy.sh` 后 GitHub Pages 开启失败（422：source 字段被错误编码为字符串）。已修复 `deploy.sh`：改用 `--input -` 传递 JSON body `{"source":{"branch":"main","path":"/"}}`。
- 新增 `index.html` 自动跳转到 `ningning_workbench.html`，让根路径 `https://chuyuecui.github.io/ningning-workbench/` 也能直接访问。
- 重新推送后 GitHub Pages 已成功开启：
  - 完整地址：https://ChuyueCui.github.io/ningning-workbench/ningning_workbench.html
  - 根地址：https://ChuyueCui.github.io/ningning-workbench/（自动跳转）
- 用户反馈 Safari 程序坞图标是默认灰色 "N"，想换外观。已生成 `icon-192.png` 与 `icon-512.png`（莫兰迪粉底 + ✨ emoji），更新 `manifest.json` 引用 PNG，并在 HTML 中添加 `apple-touch-icon` 与 `favicon`。同时升级 Service Worker 缓存版本到 v2 并清理旧缓存。已重新推送部署。
- 提示用户：Mac 上旧的程序坞图标不会自动刷新，需要从程序坞移除后重新添加；必要时 `killall Dock` 刷新缓存。
- 用户上传一张玩偶图片，希望作为 app icon。已用 PIL 将图片居中裁剪为正方形、缩放为 192×192 与 512×512，并添加圆角遮罩，覆盖保存为 `icon-192.png` 和 `icon-512.png`。重新提交并部署到 GitHub Pages。
- 提示用户：Safari/系统可能缓存旧图标，建议移除旧程序坞图标、等待 1-2 分钟后用 Safari 重新打开网址并「添加到程序坞」。
- 用户注册了 Firebase 项目 `ningning-workbench` 并提供了 firebaseConfig（apiKey=AIzaSyDAw7rPCtcXV4nZjY7AokSSp3Nsd8AjLYs, projectId=ningning-workbench, appId=1:1005980485998:web:c5f4fd809924806c47197f）。已集成 Firebase Realtime Database 到工作台：
  - 引入 firebase-app/auth/database compat SDK
  - 新增 `CloudSync` 模块：匿名登录 → 自动拉取云端数据 → DB.set 后防抖 3 秒自动上传；支持手动「立即上传 / 从云端拉取」
  - 数据同步区域 UI 改造：显示云端连接状态 + 设备 ID + 上传/拉取按钮 + 离线 JSON 备份保留
  - databaseURL 使用默认 `https://ningning-workbench-default-rtdb.firebaseio.com`（若用户选了非 us-central1 区域需调整）
  - sw.js 升级到 v3，缓存 Firebase SDK，Firebase API 请求直接回源不走缓存
- 待用户操作：在 Firebase 控制台开启 Anonymous 登录、创建 Realtime Database、设置数据库规则允许认证用户读写。
- 用户完成 Firebase 后台配置（Anonymous 已启用、Realtime Database 位于 us-central1、规则已发布）后，工作台显示「已连接云端」，电脑端数据成功上传到 `users/{uid}/workbench`。
- 用户开启 Google 登录后，新增 Google 账号登录与跨设备数据合并功能：
  - 工作台同步区域新增「使用 Google 登录」按钮与登录状态显示。
  - 实现匿名账号与 Google 账号关联（`linkWithPopup`），失败时自动降级为 `signInWithPopup` 并合并当前本地数据。
  - 登录同一 Google 账号的设备共享同一 UID，实现手机/电脑数据互通。
  - 已重新提交并部署到 GitHub Pages。
- 用户反馈 Google 登录按钮点不动、同步区域太大。已修复：
  - 给 Google 按钮内 SVG 加 `pointer-events:none`，避免图标拦截点击。
  - 将手动备份/恢复从双卡片大幅压缩为单行紧凑布局。
  - 将跨设备使用说明从多行 alert 精简为单行提示。
  - 已重新提交并部署到 GitHub Pages。

## ningning 工作台第四次迭代
- 按 2 张截图反馈微调：
  - 控制台主页：月历标题改为纯英文 "Monthly Schedule"；手动排期优先级改为 Productivity Matrix 四象限（Do / Schedule / Delegate / Delete），并同步更新图例、日期圆点颜色、任务卡片颜色，兼容旧数据。
  - 日常生活大咖舱：移除 "当前正在录入/复盘的日期" 标签，仅保留日期选择器。
- 向用户说明：PWA 安装方式、GitHub Pages 永久托管方案、每日自动轮播基于本地时区首次打开即刷新、数据存储在浏览器 localStorage（单设备），跨设备同步需 Firebase/Supabase 等云端后端。

## ningning 工作台第三次迭代
- 按 6 张截图反馈改造工作台：
  - 控制台主页：移除 AI 自动解析，改为纯手动日期 + 优先级 + 任务内容排期；月历保留并支持删除任务。
  - 工作室筹备：精简为「雷达灵感库」和「选品与孵化」两个 tab；移除供应链精算器、定价公式速查、SOP 客服话术。
  - 伴手礼包装灵感拆分为婚礼 / 商业 / 儿童三类；数字模板雷达改为展示畅销模板类型。
  - 左侧边栏 + 底部导航图标更新：✨ 主页、🌆 生活、🎀 工作室。
  - 新增 PWA 支持（manifest.json + sw.js），可安装为手机/桌面 App。
  - 新增每日自动轮播：市场新闻、灵感库、心理学知识点、法语阅读语料。
  - 新增数据导出/导入 JSON 备份功能，并说明手机电脑实时互通需接入云端数据库（Firebase/Supabase）。

## ningning 工作台第二次迭代
- 控制台主页：时间轴改为可翻页月历视图，按优先级颜色标注日期圆点。
- 日常生活大咖舱：BMI 计算新增身高输入，不再固定 162cm。
- 工作室筹备：简化为「雷达灵感库」+「选品与孵化」两个 tab，新增本地推荐选品库。
- 法语冲刺：tab 标题移除 [Input]/[Output] 标签。
- 响应式适配：左侧边栏移除外部链接，新增手机端汉堡菜单与底部固定导航。

## ningning 工作台首次创建
- 将 Streamlit 脚本转化为单文件 HTML + localStorage 工作台。
- 保留莫兰迪奶油色主题、6 大模块、localStorage 数据持久化。
